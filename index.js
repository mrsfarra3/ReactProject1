import React from "react"
import ReactDOM from "react-dom"
//Takes two arguments - what you want to render, and where you want to render it.
ReactDOM.render((<h1>Hello world</h1>), document.getElementById("root"))